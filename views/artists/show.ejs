<%- include('../partials/header', {title}) %>

<section class="show-page">
    <div>Name:</div>
    <div><%= artist.name%></div>
    <div>Genre</div>
    <div><%= artist.genre %></div>
</section>

<%- include('../partials/footer') %>